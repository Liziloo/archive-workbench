[project]
name = "archive-workbench"
version = "0.1.0"
description = "A workbench for processing archival materials for preservation and publication."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]